{
  "name": "replay-selenium-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx wait-on@latest http://localhost:3000 && node spec.js",
    "upload": "replay upload-all",
    "postinstall": "cd app && npm install",
    "dev": "cd app && npm run dev"
  },
  "type": "module",
  "keywords": [],
  "author": "Filip Hric",
  "license": "ISC",
  "dependencies": {
    "@replayio/replay": "^0.20.0",
    "selenium-webdriver": "^4.16.0"
  }
}
